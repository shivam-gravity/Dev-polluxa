"use strict";

/**
 * pricing-form-submission router
 */

const { createCoreRouter } = require("@strapi/strapi").factories;

module.exports = createCoreRouter(
  "api::pricing-form-submission.pricing-form-submission"
);
