"use strict";

/**
 * pricing-form-submission controller
 */

const { createCoreController } = require("@strapi/strapi").factories;

module.exports = createCoreController(
  "api::pricing-form-submission.pricing-form-submission"
);
